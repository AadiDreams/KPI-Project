<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Department Dashboard - Home</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="icon" type="image/png" href="/images/IRIS.png">

    <style>
        .content-area {
            padding: 20px;
        }

        .dashboard {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .widget {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .widget h3 {
            margin: 0 0 10px;
            color: #b30000;
        }

        .widget p {
            font-size: 18px;
            margin: 0;
        }

        .recent-updates {
            margin-top: 40px;
        }

        .recent-updates h3 {
            color: #b30000;
        }

        .update-item {
            background-color: #fff;
            margin-bottom: 15px;
            padding: 15px;
            border-radius: 8px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .update-item span {
            font-size: 14px;
            color: #555;
        }

        .btn {
            padding: 10px 15px;
            background-color: #b30000;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-size: 14px;
        }

        .btn:hover {
            background-color: #cc0000;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Sidebar -->
        <%- include('partials/sidebar-dept', { active: 'home' }) %>

        <!-- Main Content -->
        <main class="main-content">
            <div class="top-bar">
                <div class="profile">
                    <span>Welcome, <%= departmentName %></span>
                    <div class="profile-icon">👤</div>
                </div>
                <div class="notification">
                    <span>🔔</span>
                </div>
            </div>

            <div class="content-area">
                <h2>Welcome to the Safety Management System</h2>
                <p>Here’s an overview of your recent activities and reports.</p>

                <div class="dashboard">
                    <div class="widget">
                        <h3>Reported Incidents</h3>
                        <p><%= reportedIncidentsCount %></p>
                    </div>
                    <div class="widget">
                        <h3>Pending Actions</h3>
                        <p><%= pendingActions %></p>
                    </div>
                    
                </div>
               
            </div>
        </main>
    </div>
</body>
</html>
